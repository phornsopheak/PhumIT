<%= search_form_for @q do |f| %>
  <%= f.search_field :title, class: "form-control width-text", placeholder: t("label.search_for") %>
  <span class="input-group-btn">
    <%= f.submit t("label.go"), class: "btn btn-primary no-radius" %>
  </span>
<% end %>
